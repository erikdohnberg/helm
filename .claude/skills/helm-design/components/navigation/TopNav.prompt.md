The app masthead. The mark sits in the 104px rail so it lines up with the rail numbering used on record surfaces.

```jsx
<TopNav active="/outcomes" onNavigate={setPath} trailing={<Button variant="ghost" size="sm">Sign out</Button>} />
```

Three sections — Quarter, Outcomes, Settings. There is no sidebar in Helm. The 2px brass keel line under the masthead is structural, not a drift signal; it is the only structural use of brass in the system.
